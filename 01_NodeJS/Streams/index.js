
// Stream is something that means data can flow in chunks.
// A Stream in Node.js means data flows in small chunks instead of loading everything at once.

// A buffer is a temporary physical memory space (in RAM) that stores raw binary data (bytes) before it’s processed.

